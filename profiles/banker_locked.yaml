profile_id: banker_locked
description: "Cohort-locked banker corpus contract. Counts are fixed; sample-membership lock recovery remains pending."
paper_locked: true
evidence_mode: false
allow_vendor_fallback_for_width: true
allow_adaptive_top_k: true
top_k_requested: 8
exclude_unknown_from_main_results: false
type_slug_filter: banker
cohort_gates:
  min_samples_per_family: 3
  require_mapped_family: true
  require_sha256: true
  allow_missing_package_name: true
  max_missing_package_pct: 10.0
model_list:
  - random_forest
  - balanced_random_forest
  - xgboost
  - logistic_regression
parser_overrides: {}
feature_flags:
  enable_sample_metadata_features: true
  enable_dynamic_generic_vendor_parsers: false
threshold_overrides: {}
dataset_filters:
  mode: malicious_only
paper_lock:
  contract_id: banker_locked_contract
  canonical_historical_run_id: ""
  baseline_artifact_root: artifacts/baselines
  expected_sample_count: 354
  expected_family_count: 19
  expected_type_count: 1
  expected_type_scope: banker
  material_change_abs_delta_macro_f1_gt: 0.02
  sample_id_lock_status: unavailable
  sample_id_lock_todo: "Recover banker cohort sample IDs from historical run outputs, archived snapshots, or DB-backed legacy exports before treating this as an immutable sample-membership lock."
  notes: "Counts and banker-only scope are locked for reproducibility; sample-membership lock has not yet been recovered."
