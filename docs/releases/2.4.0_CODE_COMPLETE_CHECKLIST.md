# ObsidianDroid v2.4.0 — Code-Complete Checklist

**Audit date:** 2026-07-21 (America/Chicago)  
**Audit host:** `fedora` / `secadmin` (Dell Inspiron 15 7000 Gaming — chassis notebook; treat as current primary workstation, not a verified new desktop cutover)  
**Repository:** `/home/secadmin/Laughlin/GitHub/obsidiandroid`  
**HEAD at audit:** `95c91e5` (= `origin/main`)  
**Declared package version at audit:** `2.2.0` (no `2.4.0` declaration yet)  
**Diagnostic run evidence frozen for release claims:** `20260721T231415Z__e0c43b` in reusable slot `output/runs/allcurrent_diagnostic`

This checklist is the release boundary from the desktop-takeover readiness audit.  
Do **not** treat deferred Core/DB remediation or live pipeline re-runs as code-complete blockers unless explicitly promoted.

---

## A. Must finish before Friday 5:00 PM

Concrete, testable items only.

1. **Freeze research evidence for the release tag**
   - [ ] Archive or copy slot `output/runs/allcurrent_diagnostic` for run `20260721T231415Z__e0c43b` under an immutable path (for example `output/runs/_archived/...` or Mercury artifact root) **before** any new pipeline overwrite of the reusable slot.
   - [ ] Record in release notes: dataset / feature-column / split hashes from that run; Macro-F1 ≈ 0.698; prepared 9716; permission-signal-positive 9457; governed families 206 / observed 207; governed types 14 / observed 15; training targets 169; held-out evaluated classes 132.
   - [ ] Stop condition: do not claim newer taxonomy reactivation commits (`a698f5b`…`95c91e5`) as reflected in that diagnostic model run without a fresh read-only or diagnostic re-run.

2. **Version + changelog bump to 2.4.0**
   - [ ] Update `pyproject.toml` `version`
   - [ ] Update `config/settings/app_identity.py` `APP_VERSION`
   - [ ] Update `README.md` current-version line
   - [ ] Add `docs/RELEASE_NOTES.md` (or `docs/releases/2.4.0_RELEASE_NOTES.md`) covering shipped research reporting and explicit non-claims
   - [ ] Confirm schema/report contract versions remain independent of app version where already separated

3. **Close in-repo research packaging already on `main`**
   - [ ] Confirm package-balanced / attribution / joint-sensitivity composers remain wired and documented (`7c64038`…`8b60754`)
   - [ ] Confirm protection-lane + Permission Intel enrichment + signature-aware paths remain offline/post-run read-only
   - [ ] Confirm taxonomy count clarity + backlog contracts remain the reporting vocabulary (`2bd867a`, `dd168a5`)
   - [ ] Optional low-risk: drop or document `stash@{0}` (“wip: backlog-focus and wording”) so it is not mistaken for release work

4. **Permission capability categories (only if implementable offline by Friday)**
   - [ ] Add a static mapping contract: permission token → `capability_category` (orthogonal to protection lane)
   - [ ] Emit run-scoped report tables/manifest checksums from existing permission feature artifacts (no DB writes)
   - [ ] Tests: mapping completeness for AOSP dangerous/common set; unknown → `app-defined/unknown`; determinism of emitted CSV/JSON
   - [ ] **Stop / defer:** if mapping requires live taxonomy edits, DB enrichment writes, or pipeline retrain — move to post-2.4.0

5. **Time-series data contract (preferred) + offline report (if feasible)**
   - [ ] Document timestamp precedence: `first_analyzed` / VT first submission default → `first_discovered` when present → `first_seen_in_the_wild` rare/high-value
   - [ ] Explicitly forbid treating any of the above as APK creation time
   - [ ] Offline yearly counts / category prevalence / missing-date coverage from run snapshot columns already present (`vt_first_submission_*`, `vt_first_seen_itw_*`, `effective_first_seen_*`)
   - [ ] **Stop / defer:** platform-event causal claims; campaign windows without evidence gates; package-balanced temporal if package identity coverage is incomplete

6. **Release validation green (repo-local)**
   - [ ] `git diff --check`
   - [ ] `python -m compileall -q src scripts tests`
   - [ ] `make doc-check`
   - [ ] `make verify` / `make ci-fast` (no DB-writing targets)
   - [ ] Focused research tests for any new composers added under A.4–A.5

7. **Release candidate commit + push + Git bundle**
   - [ ] Single RC commit (or small stacked commits) on `main` or release branch
   - [ ] Push to `origin`
   - [ ] Preserve Git bundle outside Git (Mercury), not as tracked content

---

## B. Release validation

Exact commands and expected outcomes (safe / non-mutating for sources and Core):

```bash
cd /home/secadmin/Laughlin/GitHub/obsidiandroid
git status -sb                    # expect clean before RC commit
git diff --check                  # expect empty
python -m compileall -q src scripts tests
make doc-check                    # OK doc hygiene
make verify                       # import smoke / selected suite green
make ci-fast                      # mirrors CI fast path; expect pass
./scripts/dev/run_tests.sh \
  tests/test_package_balanced_permission_analysis.py \
  tests/test_package_balance_attribution.py \
  tests/test_enriched_package_family_sensitivity.py
python -m obsidiandroid.governance.taxonomy_repair_receipts governance/taxonomy_repairs
# expect PASS rows; zero FAIL
```

**Do not run for code-complete:** full analysis pipeline, model training, Core migrator apply, grant apply, Mercury restore, taxonomy UPDATE scripts.

**Skipped by policy until cutover acceptance:**

| Target | Reason |
|--------|--------|
| Live pipeline / training | Mutates run slot evidence; not required for offline report ship |
| `make check-phase2b-schema` against prod apply | Risk of treating prod Core as writable; inspect-only until remediation window |
| Core migration 0004 repair / 0005 apply | Known partial ledger; deferred |
| DB restore / schema create-drop | Mercury cutover track, not code-complete |

---

## C. Documentation / versioning

- [ ] App version `2.4.0` in `pyproject.toml`, `APP_VERSION`, README
- [ ] Release notes: research platform scope; frozen papers untouched; diagnostic (not paper) metrics
- [ ] Note taxonomy reactivation receipts under `governance/taxonomy_repairs/` are historical DB ops with Git receipts — **not** a v2.4.0 “taxonomy redesign”
- [ ] Document report/composer contract versions separately from app version where applicable
- [ ] Tag process: annotated tag `v2.4.0` only after RC tests green and notes finalized (tag not required for “code complete” if RC commit is agreed)

---

## D. Desktop-transfer prerequisites

Separate from Friday code-complete, but required before promoting any machine as primary:

- [ ] Confirm target machine identity (this audit host is **Inspiron 15 7000 Gaming / chassis type 10 notebook**, CPU i7-7700HQ — not a verified new desktop)
- [ ] Clone or sync repo to `/home/secadmin/Laughlin/GitHub/obsidiandroid` (or agreed path); ownership `secadmin:secadmin`
- [ ] Python env + system deps per README / setup scripts
- [ ] Source credentials present locally (do not commit); reader accounts only for normal analysis
- [ ] MariaDB server available; sources restored into **genuine** schema names (`erebus_threat_intel_prod`, `android_permission_intel` / `_prod` as contracted) — **not** Phase 2A disposable schemas
- [ ] Mercury disk available: UUID `715f29a9-2671-477b-8c8d-515d190addb9`, label `MERCURY_DATA_V2`, ext4
  - Current fact: mounted at `/run/media/secadmin/MERCURY_DATA_V2` (udisks); `/mnt/MERCURY_DATA_V2` is **not** the active mount (placeholder/symlink only)
- [ ] Artifact roots for runs + Mercury backups inventoried
- [ ] Acceptance tests (read-only) planned; laptop remains rollback until pass

---

## E. Database acceptance (cutover track — not Friday code-complete)

Before declaring production cutover:

- [ ] Schema object counts vs recovery-validated manifests (Erebus / Permission Intel / Core)
- [ ] Views / routines smoke (read-only)
- [ ] Row-count samples vs prior receipts (catalog ≈ 20.9k class of counts from research baseline)
- [ ] Invariants: source DBs readable by pipeline reader; no writer path used in normal analysis
- [ ] Core ledger vs physical objects reconciled (see deferred 0004/0005)
- [ ] `RESULTS_PERSISTENCE_MODE=read_only`; `OBSIDIANDROID_CORE_PERSISTENCE_ENABLED=false`
- [ ] Event scheduler state documented (currently `OFF` on this host)

**Exact Core blockers (do not “fix” during code-complete):**

- `core_schema_migration` ledger shows **0001–0003 applied only**
- Physical Core tables include label/confusion and simplified result names consistent with later migrations, but **0004/0005 are not ledgered as applied**
- Results grants / Core results writer / enabling Core persistence remain out of scope for v2.4.0 unless separately authorized

---

## F. Explicitly deferred work

Do **not** treat these as Friday code-complete blockers:

- Live Core results writer and enabling Core persistence
- Production remediation of partial migration 0004 ledger + migration 0005 + grants apply
- Historical warehouse backfill / legacy warehouse persistence enablement
- Hierarchical type-to-family modeling experiment as a product claim
- Full benign-versus-malware benchmark
- Unrestricted three-way permission mining
- Live taxonomy redesign / bulk authority changes beyond already-receipted repairs
- Causal Android platform-update → malware linkage claims
- Mercury production restore + desktop promotion (parallel track)
- Re-running `allcurrent_diagnostic` solely to pick up post-`8b60754` taxonomy reactivation commits (optional post-tag refresh)

---

## G. Rollback plan

- Keep the laptop/workstation Git + Mercury backups intact until a **named** acceptance pass succeeds on the intended primary host.
- Promotion criteria: recovery-validated restores into genuine schema names; read-only app preflight green; diagnostic smoke against restored sources; Mercury mount policy documented (`/mnt` vs udisks path).
- Rollback criteria: any restore into disposable/wrong schema names; Core persistence accidentally enabled; reusable run slot overwritten without archive; credential or grant changes outside change control.
- Do not force-push; do not rewrite history; do not delete Mercury backups/receipts or run evidence during rollback.

---

## Audit reconciliation notes (facts vs prior assumptions)

| Assumption in takeover brief | Verified fact |
|------------------------------|---------------|
| “No taxonomy repair was authorized or applied” | Multiple 2026-07-22 receipted reactivations are on `origin/main` (`a698f5b`…`95c91e5`) |
| Package-balanced analysis may still be in flight | **Complete on `origin/main`** with outputs under run `e0c43b` |
| Host is the new Fedora desktop | Host identifies as **Inspiron 15 7000 Gaming** notebook |
| `/mnt/MERCURY_DATA_V2` is the Mercury mount | Disk is mounted at **`/run/media/secadmin/MERCURY_DATA_V2`**; `/mnt/MERCURY_DATA_V2` is not the active mount |

---

## Recommended v2.4.0 ship set (summary)

**MUST_SHIP:** version/docs; deterministic offline research reporting already on main; taxonomy count clarity; package-balance stack; protection/enrichment/signature-aware composers; run-slot archival guidance; code-complete checklist + release notes non-claims.

**SHOULD_SHIP_IF_LOW_RISK:** permission capability-category contract + offline report; temporal timestamp contract + yearly offline tables from existing snapshot columns.

**DEFER_POST_2_4_0:** Core writer/persistence/migration remediation; desktop/Mercury restore promotion; hierarchical/benign benchmarks; causal platform correlation; live taxonomy campaigns.
