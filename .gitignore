# Fedora / Python virtual environments
.venv/
venv/
env/
ENV/

# Secrets & local env (never commit real credentials)
.env
.env.*
!.env.example

# OS noise
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors / IDEs
.idea/
*.swp
*.swo
*~

# VS Code — ignore personal/local overrides if present (share repo settings via PR)
.vscode/settings.local.json
.vscode/*.log

# Python bytecode & caches
__pycache__/
*.py[cod]
*$py.class
.Python

# Test / coverage / type checker / linter caches
.pytest_cache/
.pytest_tmp/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.bandit
.coverage
.coverage.*
coverage.xml
htmlcov/
.hypothesis/
.tox/
.nox/

# Jupyter
.ipynb_checkpoints/

# Packaging / build outputs
build/
dist/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
*.egg
*.manifest
*.spec

# ML / experiment tracking (local runs)
wandb/
mlruns/

# Runtime output (pipeline, diagnostics, exports)
output/
logs/
*.log
None/
paper_exports/docs/cohort_*.csv
paper_exports/docs/cohort_*.json
paper_exports/docs/cohort_*.md
paper_exports/docs/locked_vs_current_overlap.csv
paper_exports/docs/locked_vs_current_overlap.json
artifacts/paper/cohort_gap_audit_latest/
artifacts/baselines/output_writer_audit.csv
# Local snapshots of completed/current pipeline runs can be large and are
# reproducibility evidence, not source-controlled application assets.
artifacts/current_run_archives/

# Local, read-only Core-migration audit reports. They contain environment-specific
# preservation evidence and must be reviewed locally before Phase 2, not committed.
docs/core_migration/inventory/

# Patch / merge artifacts
*.orig
*.rej

# Local database & SQLite scratch files
*.db
*.sqlite
*.sqlite3
database/db_config.local.py
database/local_applied/
database/sql/android_permission_intel_semantic_repair_*.sql
