"""Non-pytest QA helpers (synthetic data fuzzer, static ML call-site scan).

Pytest collections live under ``tests/``. This package holds small utilities
invoked by tests or ``run_ml_static_scan.py``, not test cases themselves.
"""
